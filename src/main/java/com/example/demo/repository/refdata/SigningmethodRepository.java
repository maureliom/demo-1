package com.example.demo.repository.refdata;

import com.example.demo.model.refdata.Signingmethod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SigningmethodRepository extends JpaRepository<Signingmethod, Long> {
}
